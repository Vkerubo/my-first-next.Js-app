"use client"
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={() => console.log("button")}>Button</button>
    </div>
  )
}

export default AddToCart
